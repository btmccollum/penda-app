<!DOCTYPE html>
<html>
  <head>
    <title>PendaApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script type="text/javascript">
      if (window.location.hash && window.location.hash == '#_=_') { window.location.hash = ''; }
    </script>
  </head>

  <body>

    <p class="navbar-text pull-right">
      <% if client_signed_in? %>
          Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'Edit profile', edit_client_path(current_user), :class => 'navbar-link' %> |
          <%= link_to 'Log out', session_path(current_user), method: :delete %>
      <% elsif business_signed_in? %>  
          Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'Edit profile', edit_business_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_session_path, method: :delete, :class => 'navbar-link'  %>
      <% else %>
          <%= link_to "Client Sign up", new_client_path, :class => 'navbar-link'  %> |
          <%= link_to "Business Sign up", new_business_path, :class => 'navbar-link'  %> |
          <%= link_to "Login", new_session_path, :class => 'navbar-link'  %>
      <% end %>
    </p>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>

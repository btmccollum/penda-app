<div class="row">
    <div class="col-md-9 col-sm-9 col-6 d-flex mr-auto">
        <h3>Projects:</h3>
    </div>

    <div class="col-md-3 col-sm-3 col-6 d-flex">
    <div class="btn-group ml-auto" role="group">
        <button id="btnGroupDrop1" type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Status
        </button>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="btnGroupDrop1">
            <%= link_to "Show All", dashboard_path, class: "js-AllProjects dropdown-item" %>
            <%= link_to "Active", {controller: "home", action: "dashboard", status: "active"}, class: "js-ActiveProjects dropdown-item" %>
            <%= link_to "Completed", {controller: "home", action: "dashboard", status: "completed"}, class: "js-CompletedProjects dropdown-item" %>
        </div>
    </div>
    </div>
</div>

<ul class="js-Projects list-group">
    <% if @projects.exists? %>
        <% @projects.each do |p| %>
            <li class="list-group-item"><strong>Title:</strong> <%= link_to p.title, project_path(p) %> | <strong>Total Time:</strong> <%= project_total_hours(p) %>  | <strong>Last Update:</strong> <%= p.last_updated %> | <strong>Status:</strong> <%= li_group_project_status(p) %></li>
        <% end %>
    <% else %>
        No current projects. Click 'New Project' to get started.
    <% end %>
</ul>